# Cloudflare Static Assets Redirects
# Note: Domain-level redirects (http->https, www->non-www) are managed in Cloudflare Dashboard
# under SSL/TLS (Always Use HTTPS) and Dynamic Redirect Rules.
# Only relative path redirects (e.g. /old /new 301) belong in this file.

